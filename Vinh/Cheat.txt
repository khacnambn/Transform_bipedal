*Lỗi Servo sập hệ thống (Model check failed)

  • Nguyên nhân: Thư viện lerobot đang ép kiểm tra mã ID phần cứng thật. Bạn khai báo trong code là dòng sts3215 (mã
  hệ thống là 777), nhưng khi cắm con sts3095 vào thì nó trả về mã 3337. Thấy số không khớp nên thư viện từ chối
  khởi động và báo lỗi.

Cách "Cheat" giải quyết:
      - Đặt tên tất cả thành "sts3215" trong file code của bạn (bipedal_left.py và bipedal.py) vì chúng dùng chung
      một kiến trúc lập trình (register map).
      - truy cập thẳng vào file mã nguồn của thư viện tại đường dẫn: ~/.local/lib/python3.12/site-packages/lerobot/motors/motors_bus.py.
      - Tìm đến khoảng dòng 502, thêm dấu # vào đầu dòng chữ raise RuntimeError(...) để tắt tính năng kiểm tra mã
      phần cứng của nó đi.
